Sectoins

Bandcamp
  play
  pause
  next
  previous
  shuffle

Chrome
  sendMessage
  keepState?

Player
  (UI updates, calls chrome, chome calls bandcamp)
  play
  autoplay
    message or event, backup would be timer
  pause
  next
  previous
  shuffle


tonght
  player only
    get all buttons working
    stub actions
    stub return msg??




playing
  global_foot2_bundle.min
    4649
      fun startListening
    5277
      this._comm.startListening()
    4952
      this._player.play()
    4897
      this.play()
          playpause ()
    6344
      this._playlist.playpause(track_id)
        ! other goodies


  Cookie com(unnication) chanel playlist started listening


shuffle only works with visible ui elements

#pagedata.dataset.blob
  ?? identities.is_admin
      part of dom, try writing to it before js uses??
  signup_querystrings
    keys = track/album id
    




